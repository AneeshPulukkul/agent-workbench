---
name: Bug report
about: Report a reproducible defect
title: "fix: "
labels: ["bug"]
assignees: []
---

## Description

Clear statement of the defect.

## Repro steps

1. `cp .env.example .env`
2. Command run (e.g. `make test`, `make up`, request + payload):
3. Commit SHA / version (`0.1.x`):

## Expected vs actual

- Expected:
- Actual (include `ErrorEnvelope` code/message, status, logs):

## Environment

- Python (`python --version`), Node (`node --version`), Docker (`docker --version`):
- OS:

## Additional context

Relevant run ID, trace ID, or `X-Request-ID` if available.
